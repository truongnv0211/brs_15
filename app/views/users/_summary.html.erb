<tr>
  <th><%= t "user.readings", count: user.readings.count %></th>
</tr>
<tr>
  <th><%= link_to (t "user.favorites", count: user.favorites.count), favorites_path(user_id: user.id) %></th>
</tr>
<tr>
  <th><%= t "user.comments", count: user.comments.count %></th>
</tr>
<tr>
  <th><%= t "user.activities", count: user.activities.count %></th>
</tr>
<tr>
  <th><%= t "user.requests", count: user.requests.count %></th>
</tr>
<tr>
  <th>
    <%= link_to (t "user.following", count: user.following.count), follow_path(id: user.id, type: "following") %>
  </th>
</tr>
<tr>
  <th><%= link_to (t "user.followers", count: user.followers.count), follow_path(id: user.id, type: "followers") %></th>
</tr>
<tr><th></th></tr>
