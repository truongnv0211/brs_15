<div class="add-favorite">
  <%= link_to t("book.add-favorite"), favorites_path(user_id: current_user.id, book_id: @book.id),
    class: "btn btn-primary btn-block", method: :post, remote: true %>
</div>
